{
  "name": "rex-e2e",
  "private": true,
  "description": "End-to-end tests driving a real Rex binary.",
  "scripts": {
    "test": "playwright test",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
